<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="swevoq.ebread.com">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity
            android:name=".Chat.View.Utility.RegistrationActivity"
            android:theme="@style/AuthTheme" />
        <activity
            android:name=".Chat.View.Utility.AuthActivity"
            android:theme="@style/AuthTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Chat.View.Utility.PasswordRecoveryActivity"
            android:theme="@style/AuthTheme" />
        <activity
            android:name=".Chat.View.Utility.ChatListActivity"
            android:theme="@style/InsideTheme">

        </activity>
        <activity
            android:name=".Chat.View.Chat.ChatActivity"
            android:theme="@style/InsideTheme" />
    </application>

</manifest>